import _katex from "katex/src/";

// interface Nd {
//   type: string;
//   mode: "math";
//   loc: never;
// }

// type Katex = typeof _katex & {
//   __parse: (katex: string) => Nd[];
// };

// const katex = _katex as Katex;

// const parse = katex.__parse;

// const ast = parse("x = \\frac{y}{z}");
// for (const node of ast) {
//   console.log(node.type);
// }
